# Run configurations

Place immutable compositions of benchmark and authorized model configs here.
Formal execution must use `scripts/run_experiment.py` so configuration hashes
and terminal status are recorded automatically.
